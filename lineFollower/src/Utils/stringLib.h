#ifndef UTILS_STRINGLIB_H_
#define UTILS_STRINGLIB_H_

#include <stdio.h>
#include <math.h>

void ftoa(float, char*, int);
int intToStr(int, char[], int);
void reverse(char*, int);

#endif /* UTILS_STRINGLIB_H_ */
